#include "LittleUtils.hpp"
#include <vector>

#define NUM_TELEPORT_INPUTS 1

struct Teleport : Module {
	// this buffer holds the values oof
	//static std::vector<float> buffer;
	static float buffer[NUM_TELEPORT_INPUTS]; // = 0.f;
};

//Teleport::buffer == ???

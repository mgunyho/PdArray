trigger button + hold button
constant, toggle
teleport?
gain
~threshold -- hetrick compare already does this
trigger -> gate of fixed length (length controlled by text field, knob or vc)

see https://github.com/alikins/Alikins-rack-plugins/blob/master/README.md and copy where applicable, e.g. text fields etc


math (~a*b + c)
expression evaluator

arbitrary envelope generator? drawing?

adsr + vca in same module



--
UI ideas:
highlight all cables connected to a module on mouse over -- separate opacity control for this
reorder attached wires by scroll or something? (hard?)
space or ctrl+space open shortcut menu
show wire/port value on mouse over -- look at that one shitty module made by that one guy which implemented something like this
restart on loaded patches
